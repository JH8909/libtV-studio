{
  "name": "@libtv/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=../../.env --import tsx --watch src/worker.ts",
    "start": "node --env-file-if-exists=../../.env --import tsx src/worker.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@libtv/db": "workspace:*",
    "@libtv/media-gateway": "workspace:*",
    "@libtv/shared": "workspace:*",
    "@libtv/storage": "workspace:*",
    "bullmq": "^5.58.5",
    "drizzle-orm": "^0.44.5"
  },
  "devDependencies": {
    "tsx": "^4.23.1",
    "typescript": "~6.0.2"
  }
}
