# LibTV Studio 操作指南

这份指南对应当前 Standalone 版本的实际界面与交互。启动项目后，打开 `http://127.0.0.1:3000`。

## 1. 功能概览

LibTV Studio 以画布为中心组织创作：文本、图片、视频和上传素材都以节点存在；节点产生的媒体自动进入素材库，并可继续作为下游节点或时间线的参考。

- **文本节点**：写脚本、分镜、图片提示词或改写文本。
- **图片节点**：文生图、图生图与多张变体选择。
- **视频节点**：文生视频、首帧视频、首尾帧视频、参考视频。
- **素材库**：保存上传和生成的图片、视频、音频，并可直接附加为参考。
- **Timeline**：剪辑、拼接、字幕、重拍、续写和导出 MP4。
- **Agent**：先把创意整理成方案和分镜，再将确认后的方案写入画布。

## 2. 开始前：配置模型

1. 点击右上角 **模型/API**。
2. 按使用的服务配置 API Key，并选择文本、Agent、图片、视频模型。
3. 保存后回到画布；节点中的模型下拉会显示可用模型。

密钥仅保存到本机的 `standalone/data/provider-settings.json`，不会写入工作流数据。

## 3. 画布与节点

### 创建、移动与选择

- **双击画布空白处**：在当前位置打开节点目录并创建节点。
- **底部 + 按钮**：打开节点目录。
- **拖动节点标题栏**：移动节点。
- **鼠标滚轮**：缩放画布；按住 `Space` 拖动或点击底部抓手：平移画布。
- **画布空白处右键**：打开画布菜单，可自动排布、运行全部、查看属性、批量展开/收起输入区等。
- **节点右键**：先选中节点，再打开同一组上下文操作。

节点之间如需建立工作流关系，从来源节点右侧端口拖到目标节点左侧端口。系统会校验重复连接与循环连接；不再通过“把一个节点拖到另一个节点上”自动连线。

### 图片、视频节点的默认与展开状态

有生成结果的图片或视频节点，默认只显示标题和预览，保持画布简洁。

1. **点击预览区域**：展开当前节点的顶部工具栏和底部输入/参数区。
2. 再次点击同一预览：收起当前节点的工具栏和输入区，但预览图片/视频始终保留。
3. 点击画布空白或其他节点：只收起刚才展开的节点；不会隐藏其生成结果，也不会自动展开被点击的另一个节点。
4. 空状态节点没有顶部工具栏；生成出图片或视频后，点击预览才显示工具栏。

展开后的顶部工具栏依次提供：**复制、属性、放大预览、下载、删除**。其中放大用于查看生成的图片；下载用于保存生成结果。

### 节点输入与参数

- 点击输入框直接输入提示词；输入时不会触发节点折叠或重绘。
- 点击模式下拉（如“文生图 / 图生图”）切换能力。
- 点击参数下拉（画幅、清晰度、数量；或时长、画幅、分辨率、声音）选择生成参数。
- 点击右下角箭头开始生成；生成中该位置变为取消按钮。

图片节点选择 **图生图** 前必须先添加一张图片参考。视频的首帧、首尾帧和参考视频模式也会在提交前检查所需参考是否齐全。

## 4. 参考素材与工作流

### 添加参考素材

1. 展开图片或视频节点。
2. 点击 **参考**。
3. 在素材库中点击要使用的素材。
4. 素材以缩略图显示在节点内，可移除或调整其参考类型。

参考素材可用于固定产品、人物、风格、构图、首帧、尾帧或声音。素材库中的已生成媒体也可以直接拖入画布成为素材节点，或通过端口连接到下游节点。

### 推荐创作流程

1. 写一个文本节点，得到脚本或分镜。
2. 创建图片节点，填写单镜头画面提示词，必要时添加产品/角色参考。
3. 确认图片后，创建视频节点并把图片作为首帧连接或参考。
4. 生成视频并加入 Timeline。
5. 在 Timeline 中调整顺序、时长、字幕和转场，再导出 MP4。

底部的 **运行全部** 会按依赖顺序执行当前画布上的生成节点。下游节点需要上游结果时，系统会先生成上游；任一步失败后依赖链会停止，避免无效消耗。

## 5. 生成状态、失败与重试

- 节点标题栏显示排队、生成中百分比、成功、失败或已取消状态。
- 生成中，已有的图片/视频预览会继续保留；不会被空白占位覆盖。
- 底部不再显示重复进度条；状态以标题栏为准。
- 失败时，画布中间顶部显示一条说明提示，**5 秒后自动消失**。
- 失败节点仍会保留输入、参考和参数。点击节点底部的 **重试** 即可再次提交。

## 6. Agent 工作流

1. 点击右上角 **Agent**。
2. 在底部输入框下展开 **Skills**，可选择 Seedance 工作流预设：产品广告、首尾帧转场、连续叙事或素材改造。预设会把对应的镜头、参考、连续性与声音要求写入输入框；补充主题后发送即可。
3. 也可以选择已配置的 Agent 模型，直接输入创意、目标时长、平台或修改意见。
4. Agent 输出创作简报和镜头方案；继续对话可调整方案。
5. 点击 **确认方案并创建首镜**，方案会转换为画布上的文本、图片、视频节点。
6. 在 Agent 面板中执行当前镜头或批量执行；视频完成后验收、修改或加入 Timeline。

选择工作流预设或确认方案本身不会立刻产生媒体费用；只有点击生成或执行镜头后才会提交图片/视频任务。

## 7. Timeline 与导出

- 从节点或素材结果使用 **加入 Timeline**。
- 选中视频/音频片段后可拖动、裁剪；`Alt + 拖动` 为 Slip Edit。
- `S`：在播放头位置切分片段。
- 片段可删除、波纹删除、复制、交叉淡化，也能作为 AI 参考创建新视频节点。
- 在右侧片段属性中可使用 **重拍此段** 或 **续写接片**。
- 点击顶部 **导出** 生成 MP4；需要本机已安装 FFmpeg。

## 8. 快捷键

| 操作 | 快捷键 |
| --- | --- |
| 适配全部节点 | `F` |
| 抓手平移 | `H` 或按住 `Space` |
| 选择模式 | `V` |
| 撤销 / 重做 | `Ctrl/Cmd + Z` / `Ctrl/Cmd + Y` |
| 删除选中节点、连线或片段 | `Delete` / `Backspace` |
| 切分选中时间线片段 | `S` |
| 关闭菜单、弹窗与预览 | `Esc` |

输入框、下拉框和脚本编辑器获得焦点时，上述删除和画布快捷键不会覆盖正常文字输入。
