# @libtv/canvas

Functional ReactFlow/Zustand canvas extracted around the architectural seams verified in TongFlow. It provides Prompt, Generation and Asset nodes, graph persistence, third-party API submission and SSE job updates. It does not include TongFlow's Python plugin/local-compute runtime.
