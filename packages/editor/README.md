# @libtv/editor

Functional frame-based Timeline package using the media identity direction verified in OpenChatCut (`sourceAssetId` + frame positions). Current scope is asset placement, track visualization, selection/removal, persistence and selected-clip AI reference handoff. Advanced NLE operations are the next selective port.
