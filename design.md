# LibTV Studio 统一设计规范

> 版本：1.2 · 适用范围：画布、节点、图片预设、素材库、提示词库、菜单、弹窗、抽屉、Agent 面板、时间线

本文中的“必须/不得”为验收要求，“建议/优先”为设计指导。视觉实现与 `INTERACTION_v1.3.md` 冲突时，以交互契约为准；新增交互必须同时在两份文件中补充状态和验收项。

## 1. 设计目标

LibTV Studio 是深色画布上的节点式创作工作台。所有界面都必须保持以下共同感受：

- **深色、克制、内容优先**：背景与容器使用中性黑灰，图片、进度和主要操作承担视觉焦点。
- **层级清晰**：画布、节点、浮层、模态层逐级抬升，不靠随意改变圆角或颜色制造层级。
- **规则少而稳定**：同一层级的菜单、卡片、输入框和按钮使用同一套半径、边框、间距和阴影。
- **操作可预期**：点击预设只加载配置，不自动发送；真正生成由发送按钮触发。浮层关闭、键盘删除、进度反馈等行为保持一致。

本文件是后续 UI 代码和新增组件的唯一视觉依据。新增样式不得自行引入新的圆角、颜色或阴影值；如确有必要，必须先补充本文件。

## 2. 视觉基线

### 2.1 色彩角色

| 角色 | 规范值 | 用途 |
| --- | --- | --- |
| Canvas | `#101010` | 主画布与点阵背景 |
| Surface | `#1b1b1b` | 节点、普通卡片、输入区 |
| Surface raised | `#242424` | 工具栏、抽屉、弹窗、菜单 |
| Surface active | `#303030` | 悬停、选中、当前菜单项 |
| Border | `#3b3b3b` | 普通容器边框 |
| Border strong | `#454545` | 浮层、弹窗、重要分隔线 |
| Text primary | `#f2f2f2` | 标题、主要内容 |
| Text secondary | `#bdbdbd` | 次要标题、普通操作 |
| Text muted | `#888888` | 辅助说明、元信息 |
| Accent | `#8fcce9` | 选中、焦点、链接、进度强调 |
| Success | `#75d5a4` | 完成、通过、可用 |
| Warning | `#f2cf8c` | 警告、需要确认 |
| Danger | `#ff8990` | 删除、失败、不可恢复操作 |

颜色按角色使用，不直接复制某个组件里的临时颜色。成功、警告和错误颜色只用于状态，不作为大面积背景。

### 2.2 圆角等级

全项目只使用以下四个等级：

| Token | 值 | 使用范围 |
| --- | ---: | --- |
| `--radius-control` | `6px` | 图标按钮、菜单项、紧凑输入控件、标签页 |
| `--radius-surface` | `8px` | 普通卡片、文本框、缩略图、二级卡片 |
| `--radius-container` | `10px` | 节点、工具栏、菜单、Popover、抽屉、弹窗 |
| `--radius-pill` | `999px` | 状态胶囊、筛选 Chip、标签、徽标 |

规则：

1. 菜单、二级菜单、Popover、卡片不能使用 `0` 或任意方角；默认使用 `--radius-container`。
2. 普通卡片内部的嵌套卡片降一级使用 `--radius-surface`，不得比外层更大。
3. 只有贴合视口边缘的全高抽屉允许外侧边缘为 `0`；抽屉内部的卡片和输入控件仍按上述等级执行。
4. 圆形只保留给明确的圆形语义，例如头像、状态点、播放/关闭图标按钮；不能用圆形代替普通菜单或卡片。
5. 禁止新增 `7px`、`9px`、`11px`、`12px`、`13px`、`14px` 等未登记的组件圆角。旧代码中的同类值应在后续 CSS 整理中归并到最近的 token。

### 2.3 间距与尺寸

统一使用 4px 基础间距：`4 / 6 / 8 / 12 / 16 / 20 / 24 / 32px`。

| 场景 | 规范 |
| --- | --- |
| 图标与文字 | 4–6px |
| 菜单项内部间距 | 8px；菜单项高度 32–36px |
| 控件组间距 | 4–8px |
| 卡片内边距 | 10–12px |
| 抽屉/弹窗内边距 | 12–16px |
| 大区块间距 | 20–24px |
| 图标按钮 | 可见尺寸 30×30px；桌面点击热区不小于 32×32px，触屏热区不小于 44×44px |
| 顶部工具栏高度 | 44px |

同一行的控件必须共用高度。不要因为文字长短为单个按钮单独增加高度。

### 2.4 边框与阴影

- 普通容器：`1px solid var(--border)`。
- 浮层、菜单、Popover、弹窗：`1px solid var(--border-strong)`。
- 选中/键盘焦点：使用浅蓝边框或 1px 外描边，不改变容器圆角。
- 画布节点阴影：`0 14px 38px rgba(0,0,0,.32)`。
- 浮层阴影：`0 18px 42px rgba(0,0,0,.45)`。
- 模态弹窗阴影：`0 28px 90px rgba(0,0,0,.58)`。

阴影只表达层级，不用于每个按钮。按钮悬停优先改变背景和边框。

### 2.5 字体与图标

- 默认字体栈：`Inter, "PingFang SC", "Microsoft YaHei", sans-serif`；代码、ID 和时间码使用 `ui-monospace, SFMono-Regular, Consolas, monospace`。
- 页面/弹窗标题 `16px / 600`，区块标题 `13px / 600`，正文 `12px / 400`，辅助文字 `11px / 400`；不得新增小于 `11px` 的可操作文字。
- 正文行高不低于 `1.5`，长提示词和脚本行高不低于 `1.6`。
- 图标统一使用项目图标库，常规尺寸 `16px`，紧凑尺寸 `14px`；同一操作在不同界面必须使用同一图标。
- 仅图标按钮必须有可访问名称；不能以 emoji、文字符号或临时 SVG 代替正式图标。

### 2.6 交互状态

所有可操作控件必须定义 `default / hover / active / focus-visible / disabled / loading` 六种状态；选择类控件另加 `selected`，危险操作另加 `danger`。

- `hover` 只提供指针提示，不承载唯一信息。
- `active` 表示按压中的瞬时状态，`selected` 表示持续选择，二者不得混用。
- `focus-visible` 使用 `2px` Accent 外描边和 `2px` 间距，不以去除 outline 作为视觉优化。
- `disabled` 保持可读，文字/图标对比不低于相邻辅助文字，并禁止 hover/active 反馈。
- `loading` 锁定会重复提交的操作，同时保留取消入口或明确说明不可取消。

## 3. 层级模型

从低到高固定为：

1. **画布层**：点阵背景、连线、普通节点。
2. **节点操作层**：节点顶部工具栏、预设工具栏、节点内控件。
3. **浮层层**：右键菜单、节点菜单、预设二级菜单、参数 Popover。
4. **抽屉层**：素材库、提示词库、属性面板、Agent。
5. **模态层**：模型/API 设置、媒体灯箱、全文脚本预览。

不同层级通过背景、边框和阴影区分；同一层级不得使用完全不同的容器形状。

层级只能通过以下 token 设置，不允许组件自行竞争 `z-index`：

| Token | 值 | 用途 |
| --- | ---: | --- |
| `--z-canvas` | `0` | 画布、连线、普通节点 |
| `--z-node-active` | `20` | 选中、拖动、连线中的节点 |
| `--z-canvas-control` | `40` | 顶部/底部画布工具栏 |
| `--z-drawer` | `70` | 抽屉、时间线 |
| `--z-topbar` | `80` | 全局顶部栏 |
| `--z-popover` | `120` | 菜单、Popover、右键菜单 |
| `--z-modal` | `200` | 模态弹窗、灯箱、全文页 |
| `--z-toast` | `240` | Toast、阻断性系统提示 |

### 3.1 浮层互斥与关闭

- 同一时刻同一触发链只允许一个菜单或 Popover 展开；打开新浮层时关闭同级旧浮层。
- 打开模态层必须关闭全部菜单和 Popover，并将键盘焦点移入模态层；关闭后焦点返回触发控件。
- `Escape` 按层级一次只关闭最上层对象；不得一次清空选择、关闭抽屉并终止无关任务。
- 点击浮层外关闭当前菜单/Popover；点击浮层内部不得触发画布选择、拖动或缩放。
- 浮层必须限制在可视安全区内，距顶部栏、底部工具栏、抽屉、时间线和视口边缘至少 `8px`；空间不足时优先翻转，其次移动，最后内部滚动。

## 4. 组件规范

### 4.1 顶部栏与工具栏

- 顶部栏使用 `44px` 高度、`--radius-control` 的按钮。
- 画布工具栏和底部缩放栏使用 `--radius-container`，内边距 4–6px，控件之间 3–4px。
- 图标按钮默认透明；悬停使用 `Surface active`；选中使用浅色背景或浅蓝边框。
- 图标按钮只保留图标，必须提供 `title` 或 `aria-label`。
- 工具栏不能混用圆形按钮、方角按钮和大圆角按钮。唯一例外是明确的圆形头像/状态点。

### 4.2 菜单、二级菜单与 Popover

所有菜单统一使用以下结构：

- 外壳：`Surface raised`、`--radius-container`、1px 强边框、浮层阴影。
- 外壳内边距：6–8px。
- 菜单项：`--radius-control`，最小高度 32px，左右内边距 8–10px。
- 图标与文字间距：8px；菜单项文字 11–12px。
- 悬停/选中：`Surface active`，不改变圆角。
- 分组标题：使用次要文字，不再额外套一个方形容器。

适用对象：

- 图片预设一级菜单和二级卡片。
- 节点新增菜单 `.node-menu`。
- 右键菜单 `.context-menu`。
- 模型、参数、参考图角色等 Popover。

图片预设二级卡片固定宽度 `180px`，只显示大标题和必要图标；说明性文字不作为重复小标题出现。

### 4.3 卡片与节点

- 节点外壳使用 `--radius-container`；节点内部图片、空状态和输出卡片使用 `--radius-surface`。
- 节点标题栏沿用外壳上方圆角，不重新定义一套更大的半径。
- 普通卡片使用 `--radius-surface`；卡片不能出现完全方角。
- 卡片之间使用 8–12px 间距；边框比背景略亮即可，不使用厚重描边。
- 生成节点、图片节点、视频节点、文本节点必须共享标题栏、边框、状态徽标和操作区规则。
- 预设名称属于节点数据，不应作为额外的说明性标题永久堆在节点头部；需要展示时使用紧凑标签或状态信息。

#### 生成节点连续外壳

- 标题栏、进度轨道和预览区必须构成一个连续外壳：宽度、左右边框和背景边界完全对齐，层与层之间不得露出画布背景。
- 标题栏只保留上方圆角；预览区只保留下方圆角。预览区顶部圆角固定为 `0`，不能被通用卡片规则覆盖。
- 标题栏取消下边框时，预览区必须同步取消上边框；两者以共享的左右边框形成一个容器，不得各自显示完整卡片阴影。
- 实时进度轨道位于标题栏和预览区之间，作为连续外壳的一部分；无进度时其高度为 `0`，不得留下空白缝隙。
- 图片、视频、文本生成节点共享同一标题高度、接缝规则、选中描边和进度位置；媒体比例只改变预览高度，不改变外壳结构。

### 4.4 输入区与表单控件

- 文本框、下拉框和输入框统一使用 `--radius-surface`、34–36px 控件高度。
- 所有会展开选项的下拉控件必须使用应用内 `app-select` 组件；禁止在生成节点、Agent、模型/API 设置和属性面板中直接使用原生 `<select>`，避免系统方形菜单破坏统一视觉。
- `app-select` 的摘要按钮使用 `--radius-control`，展开面板使用 `--radius-container`，选项使用 `--radius-control`；选中项只改变背景和边框，不改变形状。
- 大型 Prompt 文本区可增高，但不改变圆角和边框规则。
- 聚焦时只增加浅蓝边框/外描边，不能跳变为不同形状。
- 系统正向/反向提示词默认不展示在生成输入区；它们作为预设配置参与请求，需要在提示词库中查看和编辑。
- 点击图片预设只加载正向提示词、反向提示词和布局参数，不自动发送生成。

### 4.5 抽屉、弹窗与灯箱

- 抽屉：`--radius-container`；如紧贴视口侧边，仅贴边一侧可为 0。
- 弹窗：`--radius-container`，标题区、内容区、底部操作区使用 1px 分隔线。
- 灯箱媒体预览：外壳使用 `--radius-container`，媒体本身使用 `--radius-surface`；关闭按钮可为圆形，因为它是独立的图标按钮。
- 抽屉和弹窗内部不要再嵌套另一种方角大面板。嵌套内容使用 `--radius-surface` 或透明分组。

### 4.6 标签、状态和进度

- Chip、状态徽标、版本标签统一使用 `--radius-pill`。
- 状态颜色只表达状态：进行中使用 Accent，完成使用 Success，警告使用 Warning，失败使用 Danger。
- 生成进度必须展示实时百分比或明确的阶段状态；只有服务端返回真实进度时才使用 determinate 进度条，否则显示阶段动画，不伪造精确百分比。
- 动效遵循 120–220ms 的短过渡；尊重 `prefers-reduced-motion`，减少位移和循环动画。
- 确定进度使用单色 Accent 填充，只对宽度做 `280–360ms ease-out` 过渡；不得叠加无限流光，因为这会让真实百分比看起来仍在不确定等待。
- 不确定进度使用一条 `30%` 宽的柔和高光在轨道内单向移动，单次时长 `1200–1500ms`；不得闪烁、反向弹跳或改变节点尺寸。
- 进度轨道宽度必须与节点预览宽度一致，并强制裁剪内部填充；确定进度限制在 `0–100%`，不确定进度从轨道左侧外缘移动到右侧外缘，任何缩放和视频合成状态下都不得绘制到节点之外。
- 阶段、真实百分比和已用时间在标题栏状态徽标中展示。已用时间可以每秒更新，但必须原位修改文字，不得为刷新计时器重建整个节点或重启动效。
- 进度模式从不确定切换到确定时保持同一轨道位置；只切换填充行为，不出现第二条进度条。
- `prefers-reduced-motion` 下，不确定进度改为静态低透明度整轨提示；真实百分比仍通过宽度和文字表达。

### 4.7 节点状态模型

生成节点统一使用以下状态，不允许节点类型自行增加同义状态：

| 状态 | 可见内容 | 主要操作 | 退出条件 |
| --- | --- | --- | --- |
| `idle-collapsed` | 标题、预览/空状态 | 展开、选择、连接 | 用户展开或开始生成 |
| `idle-expanded` | Prompt、参考、参数、发送 | 编辑、选择预设、发送 | 收起或提交 |
| `queued` | 阶段文字、取消 | 取消 | 服务端开始/失败 |
| `running` | 真实阶段；有真实数值才显示百分比 | 取消 | 成功/失败/取消 |
| `succeeded-collapsed` | 结果预览、结果操作 | 查看、下载、重新展开 | 用户展开或重新生成 |
| `failed-expanded` | 保留输入、错误原因、重试 | 修正、重试 | 提交/收起 |

状态变化不得清空用户 Prompt、参考素材或参数。生成成功后默认收起；失败后默认展开并定位到错误说明。

### 4.8 视频播放器

- 节点和媒体灯箱使用产品内统一播放器，不显示操作系统或浏览器原生控制条。
- 播放器固定提供播放/暂停、时间、进度拖动、静音和全屏，并始终按此顺序排列。节点宽度和放大状态不得改变控件组成、顺序、控制条结构或视觉样式。
- 点击视频画面属于节点预览区操作，可以展开或收起输入区；播放器按钮和进度轨道必须隔离点击事件，不得触发节点选择、展开或画布拖动。
- 全屏只能通过明确按钮进入，不使用双击等会与节点操作冲突的隐式手势。
- hover、键盘聚焦和播放中显示完整控制层；静止且未聚焦时保持低干扰。所有控制必须支持键盘操作和可读的 `aria-label`。
- 节点内进度轨道使用无拖动圆点的 2px 白色细线，未播放部分使用低透明度白色；聚焦时只增强轨道亮度，不显示矩形焦点框。进度控件必须允许收缩并始终裁切在播放器控制层内，任何节点比例和画布缩放下都不得溢出。
- 图片与视频共用同一尺寸的媒体查看窗口，最大 1440 × 900，并在视口内保留 24px 安全边距；媒体始终以 `contain` 居中完整展示。

### 4.9 交互层级与自动收起

- `transient` 层包括下拉、参数、预设、Agent 更多菜单和画布菜单；同一时间全局只允许一个 transient 展开。
- `workspace` 层包括节点输入区、素材、属性、Agent 和时间线。切换顶部主要工具时，先关闭 transient 并收起当前节点输入区；时间线可以作为常驻例外。
- `modal` 层包括模型/API、帮助、文本全文、媒体查看和全屏预览；打开 modal 前关闭所有 transient，并收起节点输入区。同一时间只允许一个 modal。
- 点击菜单外部关闭 transient；点击菜单内部输入、滚动区域和播放器控制不触发外层关闭。
- `Escape` 按 transient → modal → workspace 的顺序逐层关闭；关闭菜单或 modal 后，焦点返回其触发器。
- 任何新菜单必须加入共享交互层管理，不允许新增局部 `document.pointerdown` 关闭名单。
- 设置弹层必须根据最近的可滚动容器决定向上或向下展开；长模型列表最大高度为 320px，内部滚动，不允许被设置卡片或弹窗底部裁切。
- 时间线主入口保留在顶部工具栏；已生成媒体的节点悬浮工具栏可提供“添加到时间线”快捷操作，但节点参数行仅放置当前生成所需的模型、参数与参考类型控件。

## 5. 预设功能专用规则

图片预设工具栏固定放在生成节点顶部，按分类组织：分镜、角色、产品、视角、光影。一级分类保持同一按钮结构，二级条目保持同一卡片结构。

预设条目选择后的统一流程：

```text
用户描述 + 参考图
        ↓
点击图片预设
        ↓
加载正向提示词、反向提示词、布局与尺寸策略
        ↓
用户确认并点击发送
        ↓
创建并排布到右侧的新节点
        ↓
显示真实生成阶段/进度
```

约束：

- 选择预设不自动生成。
- 图生图/图生视频默认继承上游媒体尺寸；用户明确修改尺寸后才解除继承。
- 新节点自动排布在上游节点右侧，避免覆盖或挤压原节点。
- 新建派生节点后，上游生成输入区自动收起；已生成节点的输入区也应保持收起状态，除非用户主动展开。
- 节点中默认不显示系统提示词、反向提示词和上游原始 Prompt，只显示用户输入；管理入口统一放在提示词库。
- 删除统一使用键盘 Delete/Backspace；节点顶部不放重复的 X 删除按钮。
- 选择预设是一次原子更新：成功时同时写入预设 ID、系统提示词、反向提示词、布局和尺寸策略；任何一项失败时保持原配置并给出错误提示。
- 预设选择后必须显示可撤销的紧凑状态标签；不得只在隐藏配置中变化，让用户无法确认当前预设。
- 同一节点只允许一个预设菜单展开；关闭菜单后焦点返回对应分类按钮。

## 6. 画布缩放、避让与响应式规则

### 6.1 缩放语义

- `75%–250%` 为编辑区：显示完整节点控件，可打开菜单和 Popover。
- `50%–74%` 为紧凑区：默认隐藏次要结果操作和长标签；用户尝试打开节点内菜单时，系统先将目标节点居中并提升到至少 `75%`，再打开菜单。
- `25%–49%` 为总览区：节点只承担识别、选择、拖动和连接，不直接打开参数、预设或模式菜单；双击节点将其居中并恢复到 `100%` 编辑。
- 缩放不得让可操作文字小于等效 `11px`，也不得让可点击目标小于等效 `32×32px`。

### 6.2 可视安全区

- 顶部栏、底部工具栏、左右抽屉和展开的时间线共同定义画布安全区。
- 展开节点、创建派生节点、适配画布和定位节点时，完整节点边界必须落在安全区内，并保留至少 `16px` 空白。
- 若节点展开后超出安全区，画布自动平移；不得让输入框、发送按钮或错误信息被固定工具栏遮挡。
- 浮层采用视口坐标定位，不随画布缩放；其触发锚点随节点移动，浮层本身保持可读尺寸。

### 6.3 视口响应

- 桌面端保持节点和浮层的紧凑密度；菜单宽度不足时优先换行，不缩小到不可读。
- 二级预设卡片保持 180px；在窄屏下允许向内侧翻转或限制在视口内，不随意变成方角全宽块。
- 抽屉在窄屏下宽度收缩，但半径、边框、内边距和标题层级不改变。
- 工具栏可隐藏文字，仅保留图标，但必须保留 `title`/`aria-label`。

## 7. 无障碍与状态

- 键盘焦点必须可见，使用浅蓝色焦点环；不能只依赖颜色表达选中状态。
- 菜单、标签页、弹窗和进度状态使用正确的 ARIA 角色和实时区域。
- 菜单支持方向键移动、`Enter/Space` 执行、`Escape` 关闭；弹窗必须限制焦点并在关闭后恢复焦点。
- 文本与背景保持足够对比度；辅助文字不得低到无法阅读。
- 加载、成功、失败和空状态都要有文字或图标辅助，不只使用颜色。
- 动效必须支持减少动态效果设置。

## 8. 现有代码迁移清单

`standalone/public/styles.css` 中的历史样式已按组件族收敛到以下目标；后续新增代码必须直接使用这些 token，不再复制旧的硬编码圆角：

| 现有对象 | 统一目标 |
| --- | --- |
| `.node-menu`、`.context-menu`、各类 Popover | `--radius-container` |
| `.floating-drawer`、`.provider-modal`、`.help-modal` | `--radius-container` |
| `.prompt-library-card`、`.asset-card`、`.provider-card` | `--radius-surface` |
| `.node textarea`、表单 input/select | `--radius-surface` |
| `.tool-btn`、菜单项、标签页按钮 | `--radius-control` |
| `.chip`、状态徽标、版本徽标 | `--radius-pill` |
| `.asset-drawer` 的贴边外壳 | 仅贴视口边缘允许 0，内部组件不继承 0 |

当前已完成的统一约束包括：所有生成节点、Agent、模型/API 设置和属性面板的下拉均使用应用内 `app-select`；预设、节点、抽屉、弹窗、提示词库和素材库共用四级圆角；高优先级历史选择器已在最终级联层归并到 token。后续新增组件仍按“先补充本规范，再实现代码”的顺序执行。

## 9. 验收标准

完成 UI 统一后，至少满足：

- 所有普通菜单、二级菜单、Popover、卡片和弹窗都具有一致的圆角层级，不再出现无意的方形大块。
- 同一层级的容器边框、背景、阴影和内边距一致。
- 圆形只出现在圆形语义组件，Chip 只使用胶囊形。
- 图片预设、提示词库、素材库和节点操作在同一视口下具有统一的视觉密度。
- 预设选择、节点创建、输入区收起、尺寸继承、进度反馈和删除行为不因组件类型不同而改变。
- 新增组件可以在本文件中找到对应的颜色、圆角、间距、状态和交互规则。
- 在 `25% / 50% / 75% / 100% / 250%` 五个缩放点，节点不被固定工具栏遮挡；低于 `75%` 时不会出现不可读的节点内浮层。
- 空白画布、节点、连线三种右键目标显示各自菜单，不继承之前的选择对象。
- `Escape` 每次只关闭当前最上层对象；关闭菜单和弹窗后焦点回到触发控件。
- 设计系统检查必须阻止未登记的圆角、颜色、阴影和 `z-index` 进入最终样式层，而不是只检查 token 是否存在。
